name: exp1b_single_request

model: Qwen/Qwen3-0.6B

workload:
  num_requests: 1
  concurrency: 1

request:
  prompt: "Explain the difference between GPU and CPU in simple terms."
  max_tokens: 128
  temperature: 0.7
  top_p: 0.8
  top_k: 20
  min_p: 0.0
  enable_thinking: false
  stream: true